//------------// Introduction to Programming Using Java: An Object-Oriented Approach//	Arnow/Weiss//------------//------------// Chapter 8 / Section 8.5 / Page 272//	The SongLibrary class//------------ //------------// Notes//	- To compile, rename (or copy) the source file to SongLibrary.java//	- This class requires the Song class.//	- !!!! Differences from the code in the text !!!!//		The function header for the lookup method in the text//			is missing a 'throws Exception' clause.//		The code in the text for method lookup invokes a method//			of the Song class named 'getName'. This should instead//			be an invocation of the 'getArtist' method.//------------import java.io.*;class SongLibrary {	SongLibrary(String songFileName) {		this.songFileName = songFileName;	}	void lookUp(String artist) throws Exception {		BufferedReader br =	new BufferedReader(								new InputStreamReader(									new FileInputStream(songFileName)));		Song song = Song.read(br);		while (song != null) {			if (artist.equals(song.getArtist()))				System.out.println(song.getTitle());			song = Song.read(br);		}	}	String songFileName;}
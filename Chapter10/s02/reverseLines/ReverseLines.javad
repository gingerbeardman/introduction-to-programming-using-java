//------------// Introduction to Programming Using Java: An Object-Oriented Approach//	Arnow/Weiss//------------// the ReverseLines program// Chapter 10, Section 10.2, Page 385import java.io.*;import java.util.*;class ReverseLines {    public static void main(String[] a) throws Exception {        BufferedReader f = new BufferedReader(                         new InputStreamReader(System.in));        Vector         v = new Vector();        String         line;        line = f.readLine();        while (line != null) {           v.addElement(line);           line = f.readLine();        }        int k;    // k== the index of the next position in the Vector to print.                  // Positions v.size()-1, v.size()-2, ..., k+1 have already been                   //      printed.                  // Positions k,k-1, ..., 0 remain to be printed        k = v.size()-1;        while (k != -1) {           System.out.println(v.elementAt(k));           --k;        }        // k==-1 and therefore v.size()-1, v.size()-2, ..., 0 have already         //      been printed.    }}
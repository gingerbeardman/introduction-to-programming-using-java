//------------// Introduction to Programming Using Java: An Object-Oriented Approach//	Arnow/Weiss//------------// the search method for Arrays// Chapter 10, Section 10.9, Page 423-424// MODIFICATIONS://    search is made static here so we can test it without creating an object//    main creates a Array of Strings from input and then tests to see if//	each String can be found in the Array.import java.io.*;import java.util.*;class TestArraySearch {	static boolean search(String s) {// Returns true if and only if s equals one 					//      of the Strings in v.		int k;		// k== the index of the next position in 				//      the array to check.				// No match has been found in positions 				//      0 through k-1.		k = 0;		while (k!=v.length && !s.equals(v[k]))			k++;		// k==v.length || s.equals(v[k])		return k!=v.length;	}	public static void main(String[] a) throws Exception {		boolean fails=false;		BufferedReader f = new BufferedReader(new InputStreamReader(System.in));		v = new String[7];		int k=0;		String line = f.readLine();		String huge = "";		while (k<7 && line != null) {			huge = huge.concat(line);			v[k] = line;			line = f.readLine();			k++;		}		k=0;		while (k != v.length) {			if (!search(v[k])) {				System.err.println("search failure");				fails = true;			}			k++;		}		if (search(huge)) {			System.err.println("search failure");			fails = true;		}		if (!fails)			System.out.println("search passes these tests");	}	static String[] v;}// Sample test input (no duplicates allowed!)// butter// mustard// ketchup// abc// x// z// antidisestablishmentarianism
		// v is known to contain at least one element.		Enumeration    e = v.elements();		String  s;		String  longest;	// The longest String encountered so far.		longest  = (String) e.nextElement();	// The first string is always the 							//      longest up to that point.		while (e.hasMoreElements()) {			s = (String) e.nextElement();			if (s.length()>longest.length())				longest = s;		}		System.out.println("Longest String is ".concat(longest));